# .npmrc
# Next.js 16 is newer than some packages' declared peer dep ranges.
# legacy-peer-deps instructs npm to use the v6 algorithm (install anyway)
# rather than refusing. The packages themselves are compatible.
legacy-peer-deps=true
